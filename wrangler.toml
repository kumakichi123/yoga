name = "yoga-api"
main = "dist/worker/index.js"
compatibility_date = "2024-10-01"
workers_dev = true
compatibility_flags = ["nodejs_compat"]

[build]
command = "npm run worker:build"

[vars]
# DIFY_BASE は公開しても問題ない値なのでここに記載。秘密キーは `wrangler secret put` で登録する。
DIFY_BASE = "https://api.dify.ai"
